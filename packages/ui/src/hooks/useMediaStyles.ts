export const useMediaStyles = () => {
  // Default values — use Tailwind responsive classes in JSX instead
  const columnWidth = '95%';
  const headingSize = '125%';
  const primaryButtonSize = 'sm';

  return {
    columnWidth,
    headingSize,
    primaryButtonSize,
  };
};
